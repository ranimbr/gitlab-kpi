# Requirements de développement
# Installez avec : pip install -r requirements-dev.txt
#
# [SENIOR] Séparation requirements prod / dev :
#   requirements.txt     → ce qui tourne en production (minimal)
#   requirements-dev.txt → outils de dev uniquement (jamais en prod)
#
# Cette séparation réduit la surface d'attaque en production
# et acelère le build Docker (~30% plus petit sans les outils de test).

-r requirements.txt  # hérite de toutes les dépendances prod

# ── Tests ────────────────────────────────────────────────────────────────────
pytest==8.3.5           # runner de tests
pytest-asyncio==0.24.0  # support tests async (FastAPI/httpx)
pytest-cov==5.0.0       # couverture de code
httpx==0.27.0           # client HTTP pour les tests d'intégration (TestClient)

# ── Qualité de code ──────────────────────────────────────────────────────────
ruff==0.6.9             # linter + formatter ultra-rapide (remplace flake8 + black)
mypy==1.13.0            # type checking statique

# ── Debug ────────────────────────────────────────────────────────────────────
ipython==8.28.0         # REPL amélioré pour debug interactif
rich==13.9.4            # pretty-print pour debug (tracebacks colorés)
